/**
 * Will return an array with the elements the 2 arrays have in common.
 * @param firstArray
 * @param secondArray
 */
declare const arrayIntersection: <const T>(firstArray: T[], secondArray: T[]) => T[];
export default arrayIntersection;
