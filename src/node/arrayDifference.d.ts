/**
 * Will return an array with elements that are present in the first array but not in the second array.
 * @param firstArray
 * @param secondArray
 */
declare const arrayDifference: <const T>(firstArray: T[], secondArray: T[]) => T[];
export default arrayDifference;
